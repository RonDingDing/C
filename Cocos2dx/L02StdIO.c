#include <stdio.h>

int main(int argc, const char *argv[])
{
    // printf("Hello, %d\n", 100);
    // puts("Hello C\n");  //仅仅输出字符串，不能控制格式。
    // char buf[100];
    // gets(buf);
    // puts(buf);
    int a;

    scanf("%d", &a);
    printf("%d\n", a);

    return 0;
}
